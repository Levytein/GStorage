<% if (errors && errors.length > 0) { %>
    <div class="errorListWrapper" style="color: red; margin-bottom: 10px;">
      <ul class="errorList">
        <% errors.forEach(error => { %>
          <li><%= error.msg %></li> 
        <% }); %>
      </ul>
    </div>
  <% } %>